import {StyleSheet} from 'react-native';

const styles = StyleSheet.create({
  authInputTopSpace: {
    marginTop: 20,
  },
  separator: {
    marginTop: 15,
  },
  orSeparator: {
    textAlign: 'center',
    marginTop: 20,
  },
  bottomContainer: {
    paddingHorizontal: 5,
  },
  button: {
    marginTop: 35,
  },
  alertContainer: {
    paddingBottom: 10,
  },
});

export default styles;
