import colors from './colors';

const boxShadow = {
  //shadow iOS
  shadowColor: colors.gray[350],
  shadowOffset: {
    width: 0,
    height: 2,
  },
  shadowOpacity: 0.4,
  elevation: 4,
};

export default boxShadow;
