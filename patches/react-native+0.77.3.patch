diff --git a/node_modules/react-native/ReactAndroid/hermes-engine/build.gradle.kts b/node_modules/react-native/ReactAndroid/hermes-engine/build.gradle.kts
index d8b0d5f..0ac803a 100644
--- a/node_modules/react-native/ReactAndroid/hermes-engine/build.gradle.kts
+++ b/node_modules/react-native/ReactAndroid/hermes-engine/build.gradle.kts
@@ -27,11 +27,16 @@ val cmakeBinaryPath = "${cmakePath}/bin/cmake"
 fun getSDKPath(): String {
   val androidSdkRoot = System.getenv("ANDROID_SDK_ROOT")
   val androidHome = System.getenv("ANDROID_HOME")
-  return when {
-    !androidSdkRoot.isNullOrBlank() -> androidSdkRoot
-    !androidHome.isNullOrBlank() -> androidHome
-    else -> throw IllegalStateException("Neither ANDROID_SDK_ROOT nor ANDROID_HOME is set.")
+  if (!androidSdkRoot.isNullOrBlank()) return androidSdkRoot
+  if (!androidHome.isNullOrBlank()) return androidHome
+  val localPropsFile = rootProject.file("local.properties")
+  if (localPropsFile.exists()) {
+    val content = localPropsFile.readText()
+    val regex = Regex("sdk\\.dir=(.+)")
+    val sdkDir = regex.find(content)?.groupValues?.get(1)?.trim()
+    if (!sdkDir.isNullOrBlank()) return sdkDir
   }
+  throw IllegalStateException("Neither ANDROID_SDK_ROOT nor ANDROID_HOME is set.")
 }
 
 fun getSDKManagerPath(): String {
@@ -48,6 +53,13 @@ fun getSDKManagerPath(): String {
   }
 }
 
+fun getSDKManagerPathOrNull(): String? =
+  try {
+    getSDKManagerPath()
+  } catch (_: Exception) {
+    null
+  }
+
 val reactNativeRootDir = project(":packages:react-native:ReactAndroid").projectDir.parent
 val customDownloadDir = System.getenv("REACT_NATIVE_DOWNLOADS_DIR")
 val downloadsDir =
@@ -122,11 +134,15 @@ val unzipHermes by
 // `ConfigureCMake*` tasks depend upon the `ImportHermesc.cmake` file which is actually generated by
 // the two tasks mentioned before, so we install CMake manually to break the circular dependency.
 
+val sdkManagerPathOrNull = getSDKManagerPathOrNull()
 val installCMake by
     tasks.registering(Exec::class) {
-      onlyIf { !File(cmakePath).exists() }
+      onlyIf { !File(cmakePath).exists() && sdkManagerPathOrNull != null }
       commandLine(
-          windowsAwareCommandLine(getSDKManagerPath(), "--install", "cmake;${cmakeVersion}"))
+          if (sdkManagerPathOrNull != null)
+            windowsAwareCommandLine(sdkManagerPathOrNull, "--install", "cmake;${cmakeVersion}")
+          else
+            listOf("true"))
     }
 
 val configureBuildForHermes by
diff --git a/node_modules/react-native/local.properties b/node_modules/react-native/local.properties
new file mode 100644
index 0000000..7ea3ea8
--- /dev/null
+++ b/node_modules/react-native/local.properties
@@ -0,0 +1,2 @@
+sdk.dir=/Users/daniel/Library/Android/sdk
+ndk.dir=/Users/daniel/Library/Android/sdk/ndk/28.2.13676358
diff --git a/node_modules/react-native/settings.gradle.kts b/node_modules/react-native/settings.gradle.kts
index 283d16b..52bdb66 100644
--- a/node_modules/react-native/settings.gradle.kts
+++ b/node_modules/react-native/settings.gradle.kts
@@ -12,6 +12,7 @@
 // More on this here: https://reactnative.dev/contributing/how-to-build-from-source
 
 pluginManagement {
+  includeBuild("../@react-native/gradle-plugin")
   repositories {
     mavenCentral()
     google()
